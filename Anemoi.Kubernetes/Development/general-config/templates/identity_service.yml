apiVersion: v1
kind: Service
metadata:
  name: {{.Values.services.identity_port}}
  namespace: {{.Values.namespace}}
spec:
  selector:
    app: identity
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080